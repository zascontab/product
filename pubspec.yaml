name: rantipay_app
description: "RantiPay Incidence Mobile - Clean Architecture V2"
publish_to: 'none'

version: 2.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # State Management
  bloc: ^8.1.2
  flutter_bloc: ^8.1.3
  hydrated_bloc: ^9.1.5
  provider: ^6.1.1

  # Card
  shimmer: ^3.0.0

  # Dependency Injection
  get_it: ^7.6.4
  injectable: ^2.3.2

  # Functional Programming
  #fpdart: ^1.1.0
  dartz: ^0.10.1
  equatable: ^2.0.5

  # ✅ NEW: Google Sign-In Native SDK (replaces WebView OAuth)
  google_sign_in: ^6.3.0

  # Media & Photo
  #photo_view: ^0.15.0
  camera: 0.10.3+2

  # ========== UPGRADED DEPENDENCIES (FluffyChat compatibility) ==========
  # BREAKING CHANGES - Review migration guides
  file_picker: ^10.3.3          # Upgraded from ^8.1.2 - BREAKING
  permission_handler: ^12.0.1   # Upgraded from ^11.3.1 - BREAKING
  geolocator: ^14.0.2           # Upgraded from ^10.1.0 - BREAKING
  flutter_local_notifications: ^19.5.0  # Upgraded from ^17.2.3 - BREAKING
  device_info_plus: ^12.2.0     # Upgraded from ^10.1.2 - BREAKING
  share_plus: ^12.0.1           # Upgraded from ^10.1.2 - BREAKING
  go_router: ^16.2.5            # Upgraded from ^14.2.0 - BREAKING - Major navigation changes

  # Minor/Patch upgrades (safe)
  just_audio: ^0.10.5           # Upgraded from ^0.9.40
  video_player: ^2.9.5          # Upgraded from ^2.9.3
  flutter_secure_storage: ^9.2.4  # Upgraded from ^9.0.0
  app_links: ^6.4.1             # Upgraded from ^6.4.0
  record: ^6.1.1                # Upgraded from ^6.0.0
  wakelock_plus: ^1.3.3         # Upgraded from ^1.2.8
  http: ^1.5.0                  # Upgraded from ^1.1.0
  #path_provider: ^2.1.2         # Upgraded from ^2.1.1
  sqflite_common_ffi: ^2.3.6    # Upgraded from ^2.3.4
  # ========================================================================

  # Contacts for Valkyrie Chat phone search (updated package)
  flutter_contacts: ^1.1.9

  # Maps & Geolocation - FASE 2 Migration
  google_maps_flutter: ^2.5.0

  # Cache & File Management
  flutter_cache_manager: ^3.3.1
  file_saver: ^0.2.14
  cached_network_image: ^3.2.3
  open_file: ^3.2.1

  # Phone number input
  intl_phone_number_input: ^0.7.5

  # Design & Animation
  confetti: ^0.8.0

  # Image handling
  flutter_image_compress: ^2.1.0
  image_picker: ^1.2.0          # Upgraded from ^1.0.2 (FluffyChat version)
  multi_image_picker_view: ^1.0.0

  # OTP & Authentication
  pinput: ^5.0.1
  otp_autofill: ^4.1.0
  sms_autofill: ^2.3.2
  clipboard: ^0.1.3
  pin_code_fields: ^8.0.1

  # PDF handling
  pdfx: ^2.8.0
  pdf: ^3.11.1
  intl: ^0.20.2
  screenshot: ^3.0.0

  # Navigation & Web
  webview_flutter: ^4.4.2
  url_launcher: ^6.3.2

  # Environment
  flutter_dotenv: ^5.0.2

  # Network
  dio: ^5.4.0
  dio_cookie_manager: ^3.1.1
  cookie_jar: ^4.0.8
  web_socket_channel: ^2.4.0
  connectivity_plus: ^5.0.2
  internet_connection_checker: ^1.0.0+1

  # Push Notifications & Firebase
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3

  # Background Processing & Services
  flutter_background_service: ^5.0.10

  # Performance & Memory Management
  synchronized: ^3.1.0+1

  # Event Bus for inter-module communication
  rxdart: ^0.27.7

  # Collection utilities
  collection: ^1.18.0

  # Local Storage
  shared_preferences: ^2.2.2
  sembast: ^3.7.1

  # Animation
  flutter_rating_bar: ^4.0.1
  visibility_detector: ^0.4.0+2

  # Database
  drift: ^2.16.0
  sqlite3: ^2.4.0  # ⚠️ CRITICAL: Only for Dart API, NOT for native libs (uses sqlcipher via override)
  # sqlite3_flutter_libs: ^0.5.18  # ❌ REMOVED: Conflicts with sqlcipher_flutter_libs
  path: ^1.8.3
  sqflite: ^2.3.3+1

  # Code Generation
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1

  # UI Components
  cupertino_icons: ^1.0.8
  loading_animation_widget: ^1.2.0+4
  fl_chart: ^0.66.2
  google_fonts: ^6.2.1
  font_awesome_flutter: ^10.7.0
  animated_text_kit: ^4.2.2
  another_flushbar: ^1.12.31-dev.1
  flutter_typeahead: ^5.2.0
  transparent_image: ^2.0.1
  flutter_svg: ^2.2.0
  dotted_border: ^3.1.0
  dotted_line: ^3.2.3
  flutter_spinkit: ^5.2.2
  lottie: ^3.3.1
  carousel_slider: ^5.1.1
  flutter_hooks: ^0.21.3+1

  # Security & Auth
  jailbreak_root_detection: "^1.1.6"
  local_auth: ^2.3.0
  jwt_decoder: ^2.0.1
  crypto: ^3.0.3
  uuid: ^4.4.0
  encrypt: ^5.0.3

  # Device & Platform
  device_preview: ^1.1.0

  # Utilities
  currency_formatter: ^2.3.0
  flexible_polyline_dart: ^1.0.1
  printing: ^5.11.1
  mobile_scanner: ^3.5.6

  # ========== FLUFFYCHAT / VALKYRIE CHAT DEPENDENCIES (PHASE 1) ==========
  # Matrix Protocol & Chat Core
  matrix: ^3.0.1                          # Matrix SDK - Core dependency
  flutter_vodozemac: ^0.3.0               # End-to-end encryption library

  # UI Components for Chat
  animations: ^2.1.0                      # Smooth animations
  badges: ^3.1.2                          # Chat badges
  dynamic_color: ^1.8.1                   # Material You colors
  emoji_picker_flutter: ^4.3.0            # Emoji picker
  emojis: ^0.9.9                          # Emoji utilities
  flutter_highlighter: ^0.1.1             # Code syntax highlighting
  flutter_linkify: ^6.0.0                 # URL detection and linkification
  linkify: ^5.0.0                         # Core linkify library
  qr_image: ^1.0.0                        # QR image generation
  pretty_qr_code: ^3.5.0                  # QR code generation
  swipe_to_action: ^0.3.0                 # Swipe gestures
  scroll_to_index: ^3.0.1                 # Scroll to specific messages

  # Media & File Handling for Chat
  blurhash_dart: ^1.2.1                   # Image placeholders
  chewie: ^1.13.0                         # Video player controller
  archive: ^4.0.7                         # File compression
  cross_file: ^0.3.4+2                    # Cross-platform file handling
  file_selector: ^1.0.4                   # File selection
  native_imaging: ^0.2.0                  # Image processing
  image: ^4.1.7                           # Image manipulation
  mime: ^2.0.0                            # MIME type detection
  opus_caf_converter_dart: ^1.0.1         # Audio conversion

  # Platform Integration for Chat
  flutter_foreground_task: ^9.1.0         # Background service
  flutter_new_badger: ^1.1.1              # App badge notifications
  flutter_shortcuts_new: ^2.0.0           # App shortcuts
  receive_sharing_intent: ^1.8.1          # Share target functionality
  unifiedpush: ^6.2.0                     # Push notifications
  unifiedpush_ui: ^0.2.0                  # Push UI components
  package_info_plus: ^8.3.1               # App version info

  # Desktop Support for Chat
  desktop_drop: ^0.7.0                    # Drag & drop files
  desktop_notifications: ^0.6.3           # Desktop notifications
  handy_window: ^0.4.0                    # Window management

  # Chat Utilities
  slugify: ^2.0.0                         # URL-safe strings
  punycode: ^1.0.0                        # Domain encoding
  universal_html: ^2.2.4                  # HTML parsing
  html: ^0.15.4                           # HTML utilities
  tor_detector_web: ^1.1.0                # Privacy detection
  async: ^2.11.0                          # Async utilities
  webrtc_interface: ^1.3.0                # WebRTC interfaces (no heavy impl)
  flutter_web_auth_2: ^3.1.1              # OAuth authentication
  sqlcipher_flutter_libs: ^0.6.8          # Encrypted database

  # ========== FLUFFYCHAT UNIQUE DEPENDENCIES (Phase 2 - Full Integration) ==========
  # Now fully integrated - no longer using separate fluffychat package
  video_compress: ^3.1.4                  # Video compression for chat
  flutter_webrtc: ^1.2.0                  # WebRTC video/audio calls
  flutter_map: ^8.2.2                     # Maps for location sharing
  latlong2: ^0.9.1                        # Geographic coordinates
  qr_code_scanner_plus: ^2.0.13           # QR code scanner for login
  # ================================================================================

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

  # Code Generation
  build_runner: ^2.4.7
  freezed: ^2.4.6
  json_serializable: ^6.7.1
  injectable_generator: ^2.4.1
  drift_dev: ^2.16.0
  retrofit_generator: ^7.0.8  # Restored for compatibility
  # Testing Framework
  bloc_test: ^9.1.5
  mockito: ^5.4.4
  mocktail: ^1.0.4
  
  # Integration Testing
  integration_test:
    sdk: flutter
  
  
  # Performance Testing
  test: ^1.25.7
  
  # Test Utilities
  fake_async: ^1.3.1
  
  # Coverage (using built-in coverage tools)
  
  # Test Data Generation
  faker: ^2.1.0
  
  # Network Testing
  http_mock_adapter: ^0.6.1
  
  # Additional Testing Tools
  golden_toolkit: ^0.15.0
  
  # Mocking Services
  mocktail_image_network: ^1.2.0

# ========== DEPENDENCY OVERRIDES (FluffyChat compatibility) ==========
# Guidelines for adding a dependency override:
# 1. Don't do it if you can avoid it or fix it upstream in a manageable time
# 2. Always link an (upstream?) issue
# 3. Explain how and when this can be removed (overrides must be temporarily)
dependency_overrides:
  # https://github.com/juliansteenbakker/flutter_secure_storage/issues/920
  # Issue: Linux platform implementation crash on certain systems
  # Removal: When official package fixes Linux platform stability issues
#  flutter_secure_storage_linux:
#    git:
#      url: https://github.com/m-berto/flutter_secure_storage.git
#      ref: patch-2
#      path: flutter_secure_storage_linux
  flutter_secure_storage_platform_interface: 2.0.0

  # https://github.com/ThexXTURBOXx/flutter_web_auth_2/issues/155
  # Issue: Web authentication compatibility with Matrix OAuth flows
  # Removal: When upstream merges v1 removal and stabilizes v3.x
  flutter_web_auth_2:
    git:
      url: https://github.com/ThexXTURBOXx/flutter_web_auth_2.git
      ref: 3.x-without-v1
      path: flutter_web_auth_2
# ========================================================================

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

flutter_native_splash:
  color: '#63A4C9'
  image: assets/icon/logo.png

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Generate localization files (needed for FluffyChat L10n)
  generate: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/lang/
    - assets/data/sri_locations.json
    - assets/images/rantipay_logo.png
    - assets/images/rantipay.svg
    - assets/images/banner/promo1.png
    - assets/images/banner/promo2.jpg
    - assets/images/banner/promo_burger.jpeg
    - assets/images/banner/obras1.png
    - assets/images/banner/obras2.jpeg
    - assets/images/banner/obras3.jpeg
    - assets/images/logo.png
    - assets/images/rantipay/png/logo_all_horizontal.png
    - assets/images/rantipay/png/logo_all_vertical.png
    - assets/images/rantipay/png/logo_letter.png
    - assets/images/rantipay/png/logo_simbol.png
    - assets/images/rantipay/png/logo_without_des.png
    - assets/images/rantipay/svg/logo_all_horizontal.svg
    - assets/images/rantipay/svg/logo_all_vertical.svg
    - assets/images/rantipay/svg/logo_letter.svg
    - assets/images/rantipay/svg/logo_simbol.svg
    - assets/images/rantipay/svg/logo_without_des.svg
    - assets/.env
    - assets/images/
    - assets/lottie/
    - assets/font/
    # FluffyChat assets
    - lib/features/chat/assets/
    - lib/features/chat/assets/sounds/
    - lib/features/chat/assets/vodozemac/
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

